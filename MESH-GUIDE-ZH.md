# XR1710G 双机 Mesh 配置教程（简明版）

> 适用于本项目 v1.5 / v1.6 / v1.6.1 固件。方案：**6GHz 专线回程 + 2.4/5GHz 全屋同一个 Wi-Fi**。

## 组网后的样子

```
光猫 ── 主路由(192.168.50.1) ════ 6GHz 专线 ════ 节点(192.168.50.2)
              │                                      │
        手机/电脑连 2.4G/5G Wi-Fi ——全屋同名，自动漫游
```

- **主路由**：管上网（PPPoE/DHCP）、发 IP（DHCP）、防火墙。
- **节点**：只做无线桥接放大器，不管上网、不发 IP。
- **6GHz**：两台路由器之间的专线，手机搜不到、也不用连它。
- 两台路由器的 Wi-Fi 名称/密码**不会自动同步**，要各设一次。

## 开始前（3 条规则）

1. 两台刷**相同版本**的本项目固件。
2. **一次只开一台配置**（两台新刷的默认地址都是 192.168.50.1，同时开会冲突）。
3. 配置节点的过程中，电脑和节点之间**保留网线**，最后一步才拔。

---

## 第 1 步：配主路由（192.168.50.1）

电脑网线连主路由，打开 `http://192.168.50.1/`（root / 初始密码 password）。

1. **改管理员密码**（系统 → 管理权）。
2. **配上网**：网络 → 接口 → WAN，填宽带账号密码（PPPoE）或保持 DHCP，保存应用后确认能上网。
3. **设全屋 Wi-Fi**：网络 → 无线，改 2.4GHz 和 5GHz 的名称与密码（加密建议 WPA2/WPA3 混合）。

拿笔记下：`2.4G 名称 / 5G 名称 / 密码` ——第 2 步节点要填一模一样的。

> 5GHz 其他参数（信道 36 / EHT80 / 802.11r 等）保持固件默认即可，不用动。

## 第 2 步：配节点（改成 192.168.50.2）

**先不接家庭网络**。电脑网线单独连第二台，同样登录 `192.168.50.1`，先改管理员密码。

然后二选一：

**方法 A（推荐，一条命令）**：系统 → TTYD 终端（或 SSH）登录后粘贴：

```sh
xr1710g-role node 192.168.50.2/24 192.168.50.1
```

显示确认信息后**重启节点**。完成后节点后台变为 `http://192.168.50.2/`。

**方法 B（纯网页）**：网络 → 接口 → LAN → 修改：

| 项目 | 填/选 |
|---|---|
| IPv4 地址 | `192.168.50.2`，掩码 `/24`（255.255.255.0） |
| IPv4 网关 | `192.168.50.1` |
| DNS | `192.168.50.1` |
| DHCP 服务器 | **忽略此接口（勾选）** |
| WAN / WAN6 接口 | **删除或不启用** |

保存后浏览器会断开，改用 `http://192.168.50.2/` 重新进入。（IPv6 的 RA/DHCPv6/NDP 若有选项也设为停用。）

最后：进节点的 **网络 → 无线**，把 2.4G / 5G 的名称、加密、密码填成**和主路由完全一样**。

## 第 3 步：两台都配 6GHz 专线（参数必须一模一样）

分别在两台的后台 **网络 → 无线** 里，找到 6GHz 那张网卡上默认**关闭**的 Mesh 接口，按下表编辑后启用：

| 参数 | 填什么 |
|---|---|
| 模式 | 802.11s（Mesh Point，出厂模板已是） |
| 网络 | lan（默认已是） |
| Mesh ID | 自定义名称（**两端相同**，别抄教程示例） |
| 加密 | WPA3-SAE（默认已是） |
| 密钥 | 强密码（**两端相同**，建议 12 位以上） |
| 6GHz 信道 / 频宽 | 保持默认 **37 / EHT160**（两端一致即可） |

两台都设置完 → 各自点 **保存并应用** → 等待 1～2 分钟。

---

## 怎么算成功（3 项全绿再拔网线）

1. 任一台 SSH/TTYD 执行 `xr1710g-mesh-diag`，看到 `mesh plink: ESTAB`；
2. 主路由上 `ping -c 20 192.168.50.2` 全通；
3. 手机关掉再打开 Wi-Fi，连上后能上网，且拿到 192.168.50.x 的 IP。

✅ 拔掉节点网线，把节点放到目标位置（通电即可）。手机在两台之间走动时会在同名 Wi-Fi 间自动漫游（切换速度由手机决定，路由器只能辅助）。

## 排错速查

| 现象 | 原因 | 做法 |
|---|---|---|
| 两台后台都变成 192.168.50.1 | 地址冲突 | 断开一台，单独把节点改成 .50.2 并关 DHCP |
| Mesh 一直不 ESTAB | 两端参数不一致 | 按顺序核对：监管域 → 信道 → 频宽 → Mesh ID（区分大小写）→ SAE 密钥（区分大小写）→ 接口已启用 |
| 改对了还是不连 | SAE 连续输错触发 5 分钟保护 | 等 5 分钟再刷新状态，**不要**反复重启/改参数 |
| 节点下设备上不了网 | 节点还在发 IP 或没关 WAN | 节点关 DHCPv4/RA/DHCPv6、删 WAN；全网只允许主路由发 IP |
| 手机搜不到 6GHz 的名字 | 正常 | 6GHz 是专线回程，不是热点；手机连 2.4/5GHz |
| 改了主路由 Wi-Fi 名，节点没变 | 正常，不会自动同步 | 登录节点后台手动改成一样 |
| 想试 320MHz | 可选实验 | 两端同时改、信号好再试；不稳就一起退回 EHT160 |

## 日常管理备忘

- 主路由：`http://192.168.50.1/`　节点：`http://192.168.50.2/`
- 以后改 Wi-Fi 名称/密码：**两台各改一次**。
- 以后改 6GHz 的 Mesh ID、密钥、信道、频宽：**两台同步改**，改完任意一项都要保持一致。
