# XR1710G 双机 6GHz Mesh 无线回程配置教程

> 适用于本项目 v1.2.0 社区固件。方案为 **6GHz 单跳 802.11s 无线回程 + 2.4GHz/5GHz 终端漫游**，不是 MLO，也不是有线回程。

## 一、组网完成后的结构

- 主路由：`192.168.50.1`，负责 WAN/PPPoE、DHCP、DNS 和防火墙。
- Mesh 节点：`192.168.50.2`，关闭 WAN、DHCPv4、RA 和 DHCPv6，只作为桥接节点。
- 6GHz：仅用于两台 XR1710G 之间的 802.11s 回程。
- 5GHz：手机、电脑等高速终端接入和漫游。
- 2.4GHz：智能家居及老设备接入和漫游。

注意：OpenWrt 不会自动把主路由的 Wi-Fi 名称和密码同步到节点，两台设备必须分别设置。

## 二、开始前准备

1. 两台 XR1710G 使用相同版本的本项目固件。
2. 先只接一台设备配置，不能让两台刚恢复出厂的设备同时接入网络，因为它们默认都是 `192.168.50.1` 并开启 DHCP。
3. 全程保留电脑到正在配置设备的网线，确认 Mesh 已连接后再拔掉节点网线。
4. 提前准备一个自定义 6GHz Mesh ID 和一个强 SAE 密钥。不要照抄公开教程中的示例密钥。
5. 两台设备必须使用相同且符合所在地法规的监管域。XR1710G 三个频段共用同一个 Linux PHY，不要把三张 Radio 当作完全独立国家码使用。

## 三、配置主路由

### 1. 设置主路由角色

电脑单独连接第一台 XR1710G，登录：

- 地址：`http://192.168.50.1/`
- 用户名：`root`
- 首次管理员密码：`password`
- 2.4/5GHz 首次 AP：不预置无线密码，属于开放网络
- 6GHz 首次模板：不预置 SAE 密钥且默认关闭

先设置管理员密码。

如果上级网络通过 DHCP 获取地址，在 SSH 中执行：

```sh
xr1710g-role main-dhcp 192.168.50.1/24
```

如果使用 PPPoE，建议先在网页的 **网络 → 接口 → WAN** 中填写宽带账号和密码，确认 PPPoE 配置位于 WAN 而不是 LAN，然后执行：

```sh
xr1710g-role main-pppoe 192.168.50.1/24
```

查看待生效的角色配置：

```sh
xr1710g-role status
```

该工具只备份并提交设置，不会自行重载网络或重启。核对无误后手动重启主路由。

### 2. 设置终端 Wi-Fi

进入 **网络 → 无线**，设置2.4GHz和5GHz的名称、加密及密码。

建议基线：

- 2.4GHz：HE20；智能家居需要兼容旧设备时可选 WPA/WPA2 Personal 混合模式。
- 5GHz：channel 36、EHT80、WPA2/WPA3 Personal 混合模式。
- 5GHz 保留固件默认的 802.11k/v/r、`max_inactivity=86400` 和 `disassoc_low_ack=0`。

记录2.4GHz和5GHz的SSID、加密方式及密码，稍后在节点上填写完全相同的值。

## 四、配置 Mesh 节点

先不要把节点接入家庭局域网。电脑单独用网线连接第二台 XR1710G，登录其默认地址 `192.168.50.1`，先设置管理员密码。

在 SSH 中执行：

```sh
xr1710g-role node 192.168.50.2/24 192.168.50.1
xr1710g-role status
```

核对结果应包含：

- 节点 LAN 地址：`192.168.50.2`
- 网关和 DNS：`192.168.50.1`
- WAN：关闭
- DHCPv4 Server：关闭
- IPv6 RA、DHCPv6和NDP：关闭

确认后手动重启。以后节点后台地址为：

`http://192.168.50.2/`

如果完全使用网页配置，则在节点的 **网络 → 接口 → LAN** 中设置静态地址 `192.168.50.2/24`、网关和DNS为`192.168.50.1`，关闭LAN的DHCP服务器、RA、DHCPv6和NDP，并禁用WAN。修改LAN地址后浏览器会断开，需要改用新地址重新进入。

然后在节点的 **网络 → 无线** 中，把2.4GHz和5GHz的SSID、加密方式及密码设置成与主路由相同。5GHz还应保持相同的802.11r Mobility Domain；本固件默认值为`6616`。

## 五、在两台设备上配置6GHz回程

以下设置必须在主路由和节点上完全一致。进入各自后台的 **网络 → 无线**，找到6GHz Radio及默认关闭的Mesh接口并编辑。

### 6GHz Radio参数

本项目已验证的默认基线为：

- 监管域：按所在地法规选择，两台必须相同
- 信道：`37`
- 频宽：`EHT160`（本固件首次模板默认值）
- 请求功率：`28dBm`，实际功率以驱动和监管结果为准

如果监管规则、现场干扰和信号条件允许，可在两端同时尝试EHT320；若稳定性不理想则保持EHT160或降为EHT80。两端必须使用完全相同的频宽。

### Mesh接口参数

- 模式：`802.11s / Mesh Point`
- 网络：`lan`
- Mesh ID：两端填写完全相同的自定义名称
- 加密：`WPA3-SAE / SAE`
- 密钥：两端填写完全相同的强密码，建议至少12位
- Mesh Forwarding：开启
- MLO：关闭
- WDS：不要开启

固件首次启动时6GHz Mesh模板因为密钥为空而保持关闭。两台都填好参数后，先保存但保留网线；再分别启用6GHz Mesh接口并点击 **保存并应用**。

不要在6GHz上另外建立普通客户端AP，否则会改变本教程的专用回程结构。手机搜索不到这条6GHz回程SSID是正常现象，因为802.11s Mesh ID不是普通Wi-Fi热点。

## 六、确认组网成功

等待约1～2分钟，然后在两台路由器SSH中分别执行：

```sh
xr1710g-mesh-diag
```

成功时应看到6GHz对端及类似以下状态：

```text
mesh plink: ESTAB
```

也可以从主路由测试节点管理地址：

```sh
ping -c 20 192.168.50.2
```

确认以下项目后，才拔掉节点的临时管理网线并把节点放到目标位置：

- 两端Mesh均为`ESTAB`
- 主路由能够Ping通`192.168.50.2`
- 节点没有启动第二个DHCP服务器
- 手机连接2.4GHz或5GHz后能够正常上网
- 移动到两台路由器之间时，手机能在同名Wi-Fi间漫游

客户端最终何时切换AP由客户端决定，802.11k/v/r只能辅助漫游，不能保证所有手机都零秒切换。

## 七、常见问题

### 两台后台都变成192.168.50.1

两台设备发生了地址冲突。断开其中一台，只保留节点和电脑直连，再把节点改为`192.168.50.2`并关闭其DHCP服务。

### Mesh一直没有ESTAB

按顺序检查两端是否完全一致：

1. 监管域
2. 6GHz信道
3. EHT80/EHT160/EHT320频宽
4. Mesh ID，注意大小写
5. WPA3-SAE密钥，注意大小写和空格
6. 接口是否启用、是否加入`lan`

连续输错SAE密钥可能触发临时保护。改正确后等待约5分钟再观察，不要反复重启和改参数。

### Mesh已连接，但节点下的设备不能上网

检查节点是否关闭DHCPv4、RA和DHCPv6，网关/DNS是否指向主路由，并确认6GHz Mesh接口加入`lan`。主路由必须是全网唯一的DHCP服务器。

### 手机搜不到6GHz回程名称

这是正常的。当前6GHz配置是802.11s专用回程，不是普通AP；手机和电脑应连接2.4GHz或5GHz。

### 修改主路由Wi-Fi名称后，节点没有同步

这是正常的。OpenWrt不会自动同步节点配置，需要在节点上手动修改为相同的SSID、加密方式和密码。

### EHT320不稳定

先改善摆位，避免金属、电器和厚承重墙。如果仍不稳定，把两端同时恢复为固件默认的EHT160；仍有问题再同时降为EHT80。不能只改其中一台。

## 八、推荐的日常管理地址

- 主路由：`http://192.168.50.1/`
- Mesh节点：`http://192.168.50.2/`

以后修改SSID、密码、6GHz信道或频宽时，必须分别登录两台设备修改。涉及6GHz回程的Mesh ID、SAE密钥、监管域、信道和频宽必须始终保持一致。
